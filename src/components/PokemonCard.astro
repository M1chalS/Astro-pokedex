---
let {pokemon} = Astro.props
---

<a href=`pokemon/${pokemon.id}` class="flex flex-col w-full items-center border-gray-500 border rounded-2xl hover:bg-gray-200 cursor-pointer">
    <img class="w-28" src={pokemon.image} alt={pokemon.name} transition:name=`media-image-${pokemon.name}`/>
    <h4>{pokemon.name}</h4>
</a>